Meta tag
    - This tag defines the metadata(data about data) about the HTML document.
    - It should be placed inside the 'head' tag.
    Eg: <meta name="viewport" content="width=device-width, initial-scale=1.0"> // Setting the viewport to make your website look good on all devices